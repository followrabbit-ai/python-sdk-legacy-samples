# Rabbit Dynamic Pricing SDK (the optimizer that resolves the reservation).
rabbit-bq-job-optimizer>=0.1.16

# Mechanisms 1 and 2 use the legacy google-api-python-client discovery client,
# matching the legacy hot path. Google lists this library as in maintenance
# mode; it is used here to integrate with existing legacy code, not as a
# recommendation for new code.
google-api-python-client>=2.0.0

# Airflow example only (Mechanism 3). Google Cloud Composer 3 already ships
# these, so you typically do NOT install them yourself there. Pinned loosely for
# local experimentation; uncomment to run the DAG outside Composer.
# apache-airflow>=2.7.0
# apache-airflow-providers-google>=10.0.0
